description = "List all active and archived sessions in the current workspace"
prompt = """
Display the AI Architect session list.

Execute the following script to fetch and format the session metadata:

```bash
node "${extensionPath}/extension/bin/list-sessions.js"
```

**Windows (PowerShell):**
```powershell
node "${extensionPath}/extension/bin/list-sessions.js"
```
"""
