description = "Stop the Architect Loop and reset state"
prompt = """
You are stopping the Architect Loop.

Run the cancel script to deactivate the stop hook and clean up state:
```bash
node "${extensionPath}/extension/bin/cancel.js"
```

**Windows (PowerShell):**
```powershell
node "${extensionPath}/extension/bin/cancel.js"
```
"""
